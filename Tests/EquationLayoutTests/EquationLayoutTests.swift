    import XCTest
    @testable import EquationLayout

    final class EquationLayoutTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(EquationLayout().text, "Hello, World!")
        }
    }
